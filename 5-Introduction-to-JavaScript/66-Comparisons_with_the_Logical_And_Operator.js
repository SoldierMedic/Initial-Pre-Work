// in this lesson they had us write a fuction that would return a true value if the arguement that it's being passed is <=50 && >=25
function testLogicalAnd(val) {
  // Only change code below this line

  if (val<=50 && val >=25) {

      return "Yes";

  }

  // Only change code above this line
  return "No";
}

// Change this value to test
testLogicalAnd(10);
