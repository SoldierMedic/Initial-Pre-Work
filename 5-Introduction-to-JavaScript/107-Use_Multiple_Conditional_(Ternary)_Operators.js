// in this lesson I learned how to create multiple if statements inside the ternary property
function checkSign(num) {
  return (num>0) ? "positive" : (num ===0) ? "zero" : "negative";
}

checkSign(10);
