// in this lesson I learned the JS version of the Ternary operator. It allows us to easily make simple conditional based statements
function checkEqual(a, b) {
  return a==b ? true:false;
}

checkEqual(1, 2);
